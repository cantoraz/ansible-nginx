# Refer to the role ‘geerlingguy.nginx’.
# This file is modified from its ‘defaults/main.yml’.

---

# Used only for Debian/Ubuntu installation, as the -t option for apt.
nginx_default_release: ""

# Use the official Nginx PPA for Ubuntu, and the version to use if so.
nginx_ppa_use: false
nginx_ppa_version: stable

# The name of the nginx package to install.
nginx_package_name: "nginx"

nginx_conf_template: "nginx.conf.j2"
nginx_vhost_template: "vhost.j2"
nginx_auth_basic_template: auth_basic.j2
nginx_config_template: config.conf.j2

nginx_worker_processes: "{{ ansible_processor_vcpus | default(ansible_processor_count) }}"
nginx_worker_connections: "1024"
nginx_multi_accept: "off"

nginx_error_log: "/var/log/nginx/error.log warn"
nginx_access_log: "/var/log/nginx/access.log main buffer=16k flush=2m"

nginx_sendfile: "on"
nginx_tcp_nopush: "on"
nginx_tcp_nodelay: "on"

nginx_keepalive_timeout: "65"
nginx_keepalive_requests: "100"

nginx_server_tokens: "on"

nginx_client_max_body_size: "64m"

nginx_server_names_hash_bucket_size: "64"

nginx_proxy_cache_path: ""

nginx_extra_conf_options: ""
# Example extra main options, used within the main nginx's context:
#   nginx_extra_conf_options: |
#     env VARIABLE;
#     include /etc/nginx/main.d/*.conf;

nginx_extra_http_options: ""
# Example extra http options, printed inside the main server http config:
#    nginx_extra_http_options: |
#      proxy_buffering    off;
#      proxy_set_header   X-Real-IP $remote_addr;
#      proxy_set_header   X-Scheme $scheme;
#      proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#      proxy_set_header   Host $http_host;

nginx_remove_default_vhost: false
nginx_vhosts: []
# Example vhost below, showing all available options:
# - listen: "80" # default: "80"
#   server_name: "example.com" # default: N/A
#   root: "/var/www/example.com" # default: N/A
#   index: "index.html index.htm" # default: "index.html index.htm"
#   filename: "example.com.conf" # Can be used to set the filename of the vhost file.
#
#   # Properties that are only added if defined:
#   server_name_redirect: "www.example.com" # default: N/A
#   error_page: ""
#   access_log: ""
#   error_log: ""
#   extra_parameters: "" # Can be used to add extra config blocks (multiline).
#   template: "" # Can be used to override the `nginx_vhost_template` per host.
#   state: "absent" # To remove the vhost configuration.

nginx_upstreams: []
# - name: myapp1
#   strategy: "ip_hash" # "least_conn", etc.
#   keepalive: 16 # optional
#   servers: {
#     "srv1.example.com",
#     "srv2.example.com weight=3",
#     "srv3.example.com"
#   }

nginx_log_format: |
  '$remote_addr - $remote_user [$time_local] "$request" '
  '$status $body_bytes_sent "$http_referer" '
  '"$http_user_agent" "$http_x_forwarded_for"'

nginx_auth_basic_files: {}
# A dictionary that define user/password files
# nginx_auth_basic_files:
#   demo:
#     state: present
#     content: |
#       foo:$apr1$mEJqnFmy$zioG2q1iDWvRxbHuNepIh0 # foo:demo , generated by : htpasswd -nb foo demo
#       bar:$apr1$H2GihkSo$PwBeV8cVWFFQlnAJtvVCQ. # bar:demo , generated by : htpasswd -nb bar demo
#   unwanted:
#     state: absent:
#     content: |
#       baz:$apr1$mwR1xTJO$AXLrD4CpevU7r78L5U6u61 # baz:unwanted , generated by : htpasswd -nb baz unwanted

nginx_configs: {}
# A dictionary that define independent configuration
# proxy: |
#   state: absent
#   proxy_set_header X-Real-IP  $remote_addr
#   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for
# upstream: |
#   state: present
#   upstream foo {
#       server 127.0.0.1:8080 weight=10;
#   }
# geo: |
#   state: present
#   geo $local {
#       default 0;
#       127.0.0.1 1;
#   }
# gzip: |
#   state: present
#   gzip on
#   gzip_disable msie6

nginx_snippets: {}
# A dictionary that define configuration snippets
# nginx_snippets:
#   error_pages: |
#     error_page 500 /http_errors/500.html
#     error_page 502 /http_errors/502.html
#     error_page 503 /http_errors/503.html
#     error_page 504 /http_errors/504.html
#   accesslist_devel: |
#     allow 192.168.0.0/24
#     deny all
